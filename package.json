{
  "name": "reactts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server --ignore=src",
    "watch": "webpack --watch -d --colors --progress",
    "dev": "npm run watch & npm run serve",
    "build": "webpack -p"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^0.14.6",
    "react-dom": "^0.14.6"
  },
  "devDependencies": {
    "live-server": "^0.9.2",
    "ts-loader": "^0.7.2",
    "webpack": "^1.12.14"
  }
}
